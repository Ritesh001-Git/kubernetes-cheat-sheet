apiVersion: v1
kind: Service
metadata:
  name: notes-app-loadbalancer
  namespace: notes-app
spec:
  selector:
    app: notes-app
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer


# Check external IP:
# kubectl get svc -n notes-app
