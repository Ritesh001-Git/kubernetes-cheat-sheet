apiVersion: batch/v1
kind: job
metadata:
  name: job_name
spec:
  template:
    spec:
      containers:
      - name: container_name
        image: busybox
        command: ["echo","Hello kubernetes job"]
      restartPolicy: Never
